package com.movietime.movie_service.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // Swagger auto-scans; access via http://localhost:8081/swagger-ui.html
}
